# 更新日志

所有重要的项目更改都会记录在此文件中。

格式基于 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.0.0/)，
本项目遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

> 发布说明面向普通用户：写「对用户有什么改变」，不写内部实现细节（模块名、函数名、配置键）；
> 技术细节放进代码注释或 `docs/`。每个 Release 的正文由本文件对应版本段自动生成。

---

## [1.1.0] - 2026-09-06

### 部署形态变更
- **不再使用 Cloudflare Workers**：DeviantArt 封锁了数据中心出口（实测 Workers/Fly 的网页与官方 API 数据面都被拦），已删除原部署并改为服务器版。
- Bot 现在跑在 **DeviantArt 放行的出口**（住宅网络或检测通过的 VPS；国内服务器经 clash 等代理实测可用），支持 Docker Compose 一键部署与 getUpdates 长轮询（免域名/证书），详见仓库部署文档。

### 新增
- `/about` 命令：回复项目介绍并附 GitHub 源码仓库链接；`/start`、`/help` 保留用法说明。
- 每条媒体回复都带原作品页链接（可点击），可直接回到原页面查看或确认作者。
- 服务器版配套：30 秒出口检测脚本、代理订阅热更新与 WebUI 脚本（见仓库 `scripts/` 与部署文档）。

### 修复
- **改用 DeviantArt 官方接口**：此前的匿名网页接口被 DA 对云端出口封锁，改为官方接口并新增「数字作品 id → UUID」的存档快照映射后 Bot 恢复可用。
- 已知取舍：没有存档快照的新作品，以及 `fav.me`、`/view` 等旧式/短链（无法定位作品页）会收到明确的中文提示，请改用完整作品页网址。
- 没有链接的消息不再报错打扰：无说明的图片/贴纸静默忽略，私聊文字给用法提示，群聊保持安静；转发 Bot 自己回复的消息不再重复下载。

### 加固
- 防刷屏：同一聊天每分钟最多处理 15 个作品链接，超出会收到中文提示。
- 防重复：Telegram 超时重试、相册里多张照片重复带链接，都不会再造成重复发送。
- 更省网络请求：官方接口令牌与 UUID 映射在消息间缓存复用。

## [1.0.0] - 2026-09-06

### 功能
- **首个公开发布**：把 DeviantArt 单作品链接或 `fav.me` 短链发给 Bot，即可收到该作品的图片、视频或 GIF。
- 支持多种链接形态：普通网址、不带协议的裸链接、旧式作者子域名、`/view/{id}`、`view.php?id=` 与 `fav.me` 短链；藏在文字下方的链接也能识别。
- 一条消息可以放多个作品链接：按出现顺序逐个发送，单个失败不会影响其余作品。
- 图片、视频与 GIF 均以作品可获得的最高清晰度原样发送，视频支持拖动播放进度。
- 出错时用中文直接说明原因，例如作品不存在或已删除、需要登录或无权查看、作品超出 Telegram 大小限制等。

### 说明
- 首次使用前需由部署者完成一次配置（详见仓库 README 的「部署」章节）；对普通使用者无需任何设置。
