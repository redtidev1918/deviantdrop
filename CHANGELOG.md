# 更新日志

所有重要的项目更改都会记录在此文件中。

格式基于 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.0.0/)，
本项目遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

> 发布说明面向普通用户：写「对用户有什么改变」，不写内部实现细节（模块名、函数名、配置键）；
> 技术细节放进代码注释或 `docs/`。每个 Release 的正文由本文件对应版本段自动生成。

---

## [1.6.0] - 2026-09-06

### 新增
- **一键登录，多图作品全部未打码**：在电脑上运行 `npm run login`（设 `VPS=root@服务器`），
  浏览器里登录一次 DeviantArt，账号授权（OAuth）与网页登录状态会自动一起保存到服务器并立即生效，
  无需手动复制 Cookie、无需开放端口/域名、无需重启。登录后成熟（Mature）多图作品的**所有画面**
  （含此前被跳过的附加页）都会未打码发送；未登录时附加页仍按原策略跳过并提示去作品页查看。
- **群聊可用**：关闭 Bot 的群组隐私模式（BotFather → Group Privacy → Disable，再把 Bot 移出重加
  或设为管理员）后，群里直接发 DeviantArt 链接即可响应。
- 来源入口统一且不再占地方：单图/视频用图片下方的「🔗 在 DeviantArt 打开」按钮；多图画册在
  相册下方补发一行可点的「🔗 在 DeviantArt 打开」，且不展开链接预览。

### 修复
- 成熟作品登录后不再只发主图：此前主图会被错误降级成低清展示图、且附加页一律跳过；
  登录态下主图与附加页都发送未打码原始画面。
- 修复服务器（poll）上传通道下，说明文字含 emoji/中文时来源链接高亮错位（只亮前几个字）的问题：
  媒体说明不再放链接，单图改用按钮、画册改用补发文本链接，规避上传通道的实体偏移兼容问题。
- 同一作品重复发送不再重复下载：修复坏缓存条目与大图压缩降级导致的去重失效，file_id 重放覆盖
  单图、画册与压缩成文件的大图。
- 管理命令（`/login`、`/status`、`/cookies`）只允许 **Bot 所有者**（`ADMIN_IDS`）使用；
  未配置所有者时一律拒绝，普通使用者白名单不再等同于管理员。`/cookies` 现在引导使用一键登录。
- 出错提示不再笼统显示「服务暂时无法完成」：脱敏后保留真实原因，便于反馈定位。

## [1.5.0] - 2026-09-06

### 新增
- 管理员私聊 `/login` 打开一次性 OAuth 授权入口，保存后立即生效；`/cookies` 通过受保护表单热更新 Cookie。
- 提供轻量作品预览页与公开缩略图入口，需要配置 HTTPS 公网域名。
- 可选 TelePress 大图集/发送失败兜底已接入；关闭或失败均不影响原生 Telegram 媒体发送。

### 修复
- 失效或损坏凭据不再回退旧环境变量，写盘失败不会假报登录成功，并发请求不再重复轮换 token。
- 管理命令在未配置管理员时拒绝执行，登录链接仅在私聊发出。
- 长标题仍保留可点击来源；压缩和预览说明在缓存重发时保留；相册不再多发仅含来源按钮的消息。
- 11 张等尾部单图正确单发，修复状态命令报错和 Cookie session 热更新。
- 防止媒体重定向 SSRF；访问凭据不再进入通用持久缓存和代理日志。
- poll 同时提供 HTTP 健康检查，媒体响应流式传输，请求体有上限；自动部署先验证再执行。

### 验证与配置
- 69 项测试通过，Workers dry-run 通过；TelePress 上游鉴权/服务端 33 项测试通过。
- Web OAuth 与公开预览需要 PUBLIC_BASE_URL 和 DA 回调白名单；没有公网域名时不会宣称已完成登录验收。
- 使用既有 /data 卷，无需清空数据；升级说明见 docs/AUTH_AND_PREVIEW.md。

## [1.4.0] - 2026-09-06

### 新增
- **OAuth 用户登录（替代会过期的 Cookie）**：`npm run login` 一次授权自动写入 VPS 并重启；refresh token 支持轮换持久化，登录态长期有效。
- 成熟 / NSFW 作品用用户 token 走官方接口取**未打码原图**；匿名/登录失效时明确提示「账号未登录成熟内容，仅打码预览」。
- 自动化：新增 CI（每次 push/PR 跑测试）与 Release workflow（打 `v*` tag 自动生成 Release）；新增 `npm run deploy:vps`、`npm run logs`。

### 修复
- 修复服务器上传图片、视频、相册反复报「缺少 photo/media」的问题（上传自动重建重试，最多 3 次；相册改由 Telegram 服务端合并）。
- 原图额度受限（免费账号每日额度）时自动降级最高清展示图并注明，不再直接失败。

## [1.3.2] - 2026-09-06

### 修复
- 修复服务器上传图片、视频和相册时反复报“缺少 photo/media”的问题。
- 多张照片/视频使用真正的 Telegram 相册发送，保留各文件类型。
- 恢复照片大小限制，修复大图转文件失败；下载、压缩、发送阶段提示按顺序显示。
- 群话题回复留在原话题，频道消息也可解析；原消息删除后仍可发送结果。
- 修复多文件作品重发只返回主图、附加文件被截断的问题。
- Docker 缓存保存在独立卷中，更新容器后继续保留。
- 更新图像压缩依赖，修复其处理外部图片时的已知安全漏洞。

### 已有功能完善
- 相册里的超大照片也会先压缩再发送（此前只有单图路径压缩，相册直接报「file too big」）；压缩失败的单张降级为文件发送。
- 相册整组也做 file_id 缓存：再次发送同一多图作品时零下载、直接以相册回放。
- 「处理中」提示的阶段切换（下载 → 发送）会更及时显示，不会被节流吞掉。

## [1.2.1] - 2026-09-06

### 新增
- 支持多文件作品：主图与附加画面合并为**一条 Telegram 相册消息**（≤10 张）一次发出，不再只回第一张。
- 超过 10MB 的原图会先在服务器压缩，以内嵌图片形式发送；压缩不可用时才回退为文件发送。
- 「处理中」提示改为阶段式并带下载进度；Telegram 网络抖动会自动重试并给出中文提示。

### 修复
- 相册下载会显示每张的进度百分比（`正在下载第 x/N 张 60%`），不再只有无进度的「第 x/N 张…」。

## [1.2.0] - 2026-09-06

### 新增
- 发送媒体前先回一条**自动删除的「处理中」提示**，多链接时显示进度（`⏳ 第 x/N 个作品…`），完成后自动消失。
- **媒体级去重**：重复发同一作品链接时，直接用已上传的 Telegram file_id 重发（零下载、零重传），缓存持久化到服务器磁盘、重启不丢。
- **超大图片自动转文件**：原图超过 Telegram 照片上限时会自动以文件形式发送（最大 50 MiB），不再因过大而失败。
- **支持需登录的成熟 / NSFW 内容**：配置你的 DeviantArt 登录 Cookie 后，成熟作品也能下载未打码原图（详见仓库部署文档）。

### 修复
- 解析改为「网页接口优先、官方接口兜底」：在 DA 放行的出口（如机场代理）上，新作品不再依赖存档快照即可解析，视频/GIF 取数更完整；网页不可达时才回退官方接口。
- 原图下载修复：此前拿媒体地址拼 `/v1` 变体导致部分作品（尤其成熟/大图）下载 400/404，现直接使用原始文件地址，可正常取到最高分辨率原图。
- 需登录的成熟作品不再误报「存档缺失/HTTP 400」，而是给出可操作的明确提示（配置登录 Cookie 后即可下载）。

## [1.1.0] - 2026-09-06

### 部署形态变更
- **不再使用 Cloudflare Workers**：DeviantArt 封锁了数据中心出口（实测 Workers/Fly 的网页与官方 API 数据面都被拦），已删除原部署并改为服务器版。
- Bot 现在跑在 **DeviantArt 放行的出口**（住宅网络或检测通过的 VPS；国内服务器经 clash 等代理实测可用），支持 Docker Compose 一键部署与 getUpdates 长轮询（免域名/证书），详见仓库部署文档。

### 新增
- `/about` 命令：回复项目介绍并附 GitHub 源码仓库链接；`/start`、`/help` 保留用法说明。
- 每条媒体回复都带原作品页链接（可点击），可直接回到原页面查看或确认作者。
- 服务器版配套：30 秒出口检测脚本、代理订阅热更新与 WebUI 脚本（见仓库 `scripts/` 与部署文档）。

### 修复
- **改用 DeviantArt 官方接口**：此前的匿名网页接口被 DA 对云端出口封锁，改为官方接口并新增「数字作品 id → UUID」的存档快照映射后 Bot 恢复可用。
- 已知取舍：没有存档快照的新作品，以及 `fav.me`、`/view` 等旧式/短链（无法定位作品页）会收到明确的中文提示，请改用完整作品页网址。
- 没有链接的消息不再报错打扰：无说明的图片/贴纸静默忽略，私聊文字给用法提示，群聊保持安静；转发 Bot 自己回复的消息不再重复下载。

### 加固
- 防刷屏：同一聊天每分钟最多处理 15 个作品链接，超出会收到中文提示。
- 防重复：Telegram 超时重试、相册里多张照片重复带链接，都不会再造成重复发送。
- 更省网络请求：官方接口令牌与 UUID 映射在消息间缓存复用。

## [1.0.0] - 2026-09-06

### 功能
- **首个公开发布**：把 DeviantArt 单作品链接或 `fav.me` 短链发给 Bot，即可收到该作品的图片、视频或 GIF。
- 支持多种链接形态：普通网址、不带协议的裸链接、旧式作者子域名、`/view/{id}`、`view.php?id=` 与 `fav.me` 短链；藏在文字下方的链接也能识别。
- 一条消息可以放多个作品链接：按出现顺序逐个发送，单个失败不会影响其余作品。
- 图片、视频与 GIF 均以作品可获得的最高清晰度原样发送，视频支持拖动播放进度。
- 出错时用中文直接说明原因，例如作品不存在或已删除、需要登录或无权查看、作品超出 Telegram 大小限制等。

### 说明
- 首次使用前需由部署者完成一次配置（详见仓库 README 的「部署」章节）；对普通使用者无需任何设置。
